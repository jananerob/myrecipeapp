<div class="container mx-auto p-4">
  <h1 class="text-3xl font-bold mb-6 text-gray-800">My cookbook</h1>
  <div class="space-y-4">
    <% if @recipes.any? %>
      <%= render @recipes %>
    <% else %>
      <p class="text-gray-500">Your cookbook is empty.</p>
    <% end %>
  </div>
</div>