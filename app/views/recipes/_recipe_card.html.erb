<div class="recipe-card p-0 border-2 border-orange-100 hover:border-orange-300 rounded-2xl shadow-sm hover:shadow-lg transition-all duration-300 bg-white h-full flex flex-col justify-between overflow-hidden">
  <% if recipe.image.attached? %>
    <%= image_tag recipe.image, class: "w-full h-48 object-cover rounded-t-lg" %>
  <% else %>
    <%# Placeholder: Ak recept nemá fotku, zobrazí sa napríklad sivý box s ikonkou jedla %>
    <div class="w-full h-48 bg-gray-100 flex items-center justify-center rounded-t-lg">
      <span class="text-5xl text-gray-300">🍽️</span>
    </div>
  <% end %>

  <div class="p-4 flex-grow"> <%# Obsah karty %>
    <h3 class="text-xl font-semibold text-gray-900 mb-2">
      <%= link_to recipe.title, recipe %>
    </h3>

    <p class="text-gray-600 text-sm mb-2">
      ⏱ <%= recipe.prep_time %> min 🔥 <%= recipe.cook_time %> min 🍽 <%= recipe.calories.presence || "-" %> kcal
      <% if action_name == 'my_recipes' %>
        <%= recipe.is_private ? '🔒 Private' : '🌍 Public' %>
      <% end %>
    </p>

    <% if recipe.tags.any? %>
      <div class="flex flex-wrap gap-1 mb-4">
        <% recipe.tags.each do |tag| %>
          <span class="bg-orange-50 text-orange-700 text-xs px-2 py-0.5 rounded border border-orange-100">
            <%= tag.name %>
          </span>
        <% end %>
      </div>
    <% end %>
  </div>

  <div class="mt-4 pt-3 border-t border-gray-100 flex justify-center">
    <%= link_to recipe_path(recipe), class: "w-full py-2 text-center text-orange-600 font-semibold hover:bg-orange-50 transition-colors rounded-b-lg" do %>
      View Full Recipe
    <% end %>
  </div>
</div>
